import EmojiBlock, { EmojiAdder, EmojiCounter } from '../../dist';
import '@testing-library/jest-dom';
// import * as React from 'react';
// import { render, fireEvent, screen } from '@testing-library/react';

describe('EmojiAdder', () => {
  it('is truthy', () => {
    expect(EmojiAdder).toBeTruthy();
  });
});

describe('EmojiCounter', () => {
  it('is truthy', () => {
    expect(EmojiCounter).toBeTruthy();
  });
});

describe('EmojiBlock', () => {
  it('is truthy', () => {
    expect(EmojiBlock).toBeTruthy();
  });
});
